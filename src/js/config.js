/** The variable that we include here is the one responsible
 * for kind of defining some important data about the application itself
 * Example
 * - API URL -> This URL might used on many parts
 * - If this API need to be change, then we no need to change on each file
 * - Rather we change on this file will impact on all the file
 */

// Use uppercase variable name on the configuration file
// Named Export
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';
export const TIMEOUT_SEC = 10;
export const RES_PER_PAGE = 10;
export const KEY = 'ba95e2e6-d1a4-40c9-bf86-64873a33a428';
export const MODAL_CLOSE_SEC = 2.5;
